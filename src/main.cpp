#include <cstdlib>
#include <exception>
#include <iostream>

auto main() -> int {
    std::cout << "Hello OpenGL Triangle!\n";

    // try {
    //     app.Run();
    // } catch (const std::exception& e) {
    //     std::cerr << e.what() << "\n";
    //     return EXIT_FAILURE;
    // }

    return EXIT_SUCCESS;
}
